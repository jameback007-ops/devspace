# Do not commit real values. Copy this file outside the repository or export the
# variables through a credential-aware process supervisor.
CONTROL_PLANE_TUNNEL_ID=tunnel_0123456789abcdef0123456789abcdef
CONTROL_PLANE_API_KEY=replace-with-runtime-key-not-admin-key
TUNNEL_HEALTH_PORT=18080

# Existing standalone bridge inputs.
BRIDGE_WORKSPACE_HOST_PATH=/absolute/path/to/disposable-repository
BRIDGE_PORT=18765
